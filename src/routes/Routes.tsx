import React from "react";
import { Switch, Route, Redirect } from "react-router-dom";

const Routes = () => {
  return (
    <Switch>
      {/* <Route exact path="/" component={} />
      <Route path="/meme-generaor" component={} />
      <Redirect path="*" to="/" /> */}
    </Switch>
  );
};

export default Routes;
